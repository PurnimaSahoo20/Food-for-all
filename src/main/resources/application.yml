server:
  port: 9999
  servlet:
    session:
      cookie:
        name: SERVERCOOKIE
spring:
  web:
    resources:
      static-locations: file:uploads/, classpath:/static/
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/spring?useSSL=true
    password: Purnima@123
  application:
    base-url: http://localhost:9999/


  mail:
    host: smtp.gmail.com
    port: 587
    username: purnimasahoo235@gmail.com
    password: "eyvr iead ktvx uuqh"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


camunda:
  client:
    auth:
      method: basic
      username: demo
      password: demo
    enabled: true
    mode: selfManaged
    grpc-address: http://localhost:26500
    rest-address: http://localhost:8088